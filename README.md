# neow
A single-screen dashboard visualizing NASA's Near-Earth Object data (NeoWs API) with Chart.js: daily asteroid traffic, closest-approach distances, and a radar comparison of the week's four largest asteroids. Vanilla HTML/CSS/JS, no build step, no dependencies beyond the Chart.js CDN.
